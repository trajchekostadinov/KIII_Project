import { Box } from '@mui/material';

const Footer = () => {
    return (
        <Box
            component="footer"
            sx={{
                backgroundColor: '#635bff',
                height: '40px',
                mt: 'auto',
            }}
        />
    );
};

export default Footer;